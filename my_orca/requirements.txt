# Additional dependencies for camera-driven ORCA Hand grasping
# Core dependencies (see orca_core/pyproject.toml for the full list):
#   dynamixel-sdk, pyyaml, numpy

# Camera hand tracking
mediapipe>=0.10.0
opencv-python>=4.8.0

# Serial port enumeration (already needed by orca_core)
pyserial>=3.5
